<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sap.codelab">

    <!-- Required to post the reminder notification on API 33+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Location/geofence permissions and the geofence receivers are declared by the :geofence
         module and merged in. -->
    <!-- OpenStreetMap (osmdroid) downloads and caches map tiles over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.sap.codelab.view.home.Home"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.sap.codelab.view.create.CreateMemo"
            android:exported="false"
            android:label="@string/new_memo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.sap.codelab.view.detail.ViewMemo"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.sap.codelab.view.map.MapPickerActivity"
            android:exported="false"
            android:label="@string/map_title"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>