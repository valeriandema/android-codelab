<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location: required to register/monitor geofences. Background access lets reminders fire
         when the app is closed (implicitly granted on API < 29). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Geofences are cleared on reboot; this lets us re-register them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>

        <!-- Receives geofence transitions from Google Play Services, even in the background. -->
        <receiver
            android:name=".GeofenceBroadcastReceiver"
            android:exported="false" />

        <!-- Re-registers geofences after the device reboots. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
